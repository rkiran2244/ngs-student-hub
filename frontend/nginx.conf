server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Optional: route /uploads to backend via proxy in a staging setup
  # location /uploads/ {
  #   proxy_pass http://backend:5001/uploads/;
  # }

  gzip on;
}

# Example environment variables for frontend
# Copy this to frontend/.env (or use frontend/.env.local) and fill in values.

# API URL for backend (include protocol)
VITE_API_URL="http://localhost:5001"

# Supabase placeholders (if using Supabase integration)
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_URL=

[pytest]
# Only collect tests under backend/tests
testpaths = backend/tests
python_files = test_*.py
# Don't recurse into scripts directory which contains helper scripts that may start with test_
norecursedirs = backend/scripts
